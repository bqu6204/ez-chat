import styleSheet from "@/styles/dist/primary-footer.module.css";

const PrimaryFooter: React.FC = () => {
  return <footer className={styleSheet.footer}>foot</footer>;
};

export default PrimaryFooter;
